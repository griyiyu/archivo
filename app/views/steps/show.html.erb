<p id="notice"><%= notice %></p>

<p>
  <strong>File record:</strong>
  <%= @step.file_record.title %>
</p>

<p>
  <% if @step.person != nil %>
    <strong>Person:</strong>
    <%= @step.person.name %>
  <% end %>
</p>

<p>
  <strong>Office:</strong>
  <%= @step.office.name %>
</p>

<%= link_to 'Edit', edit_step_path(@step) %> |
<%= link_to 'Back', steps_path %>
